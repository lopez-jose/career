#include <string.h>
#include <stdio.h>

void reverseString(char *str)
{
    int l, i;
    char *begin_ptr, *end_ptr;

    l = strlen(str);

    begin_ptr = str;
    end_ptr = str;
}

int main()
{

    printf("Hello world");
    char str[100] = "hello";
    //reverseString(&s);

    return 0;
}
